# .env.example
# ------------------------------------------------------------------------
# INSTRUCTIONS:
# 1. Copy this file and rename it to .env
# 2. Replace all values containing <...> with your actual local paths/IDs.
# 3. DO NOT commit the real .env file to version control (git).
# ------------------------------------------------------------------------

# --- Google Cloud Platform Configuration ---
# Your unique GCP Project ID (found in GCP Console Dashboard)
GCP_PROJECT_ID=your-gcp-project-id

# Full absolute path to your Service Account JSON key file
# Windows Example: C:/Users/Name/.dbt/fraud-detection-key.json
# Linux/Mac Example: /home/name/.dbt/fraud-detection-key.json
DBT_KEY_PATH=/absolute/path/to/your/service-account-key.json

# --- BigQuery Configuration ---
# The dataset and table names to be created/used in BigQuery.
# WARNING: If you change these values, you MUST also update:
# 1. The 'schema' in dbt_project/models/staging/sources.yml
# 2. The 'key_prefix' in dagster_project/.../assets.py
# Failure to match these will break the Dagster lineage graph.
BQ_DATASET_ID=fraud_detection_raw
BQ_TABLE_ID=transactions

# --- Local Data Paths ---
# Directory where you downloaded the Kaggle dataset
KAGGLE_DATA_PATH=/absolute/path/to/kaggle_data_folder

# Relative path to where the script should save the processed CSV
# Default: data/raw/creditcard.csv (Relative to project root)
CSV_PATH=data/raw/creditcard.csv

# --- Orchestration (Dagster & dbt) ---
# REQUIRED: Absolute path to the 'dbt_project' folder.
# This bridges Dagster (Python) and dbt (SQL).
# Windows Example: D:/projects/fraud-pipeline/dbt_project
DBT_PROJECT_DIR=/absolute/path/to/your/repo/dbt_project

# --- Docker Infrastructure (Postgres) ---
# Credentials for the local metadata database used by Dagster.
# These are injected into the container at runtime.
POSTGRES_USER=dagster_user
POSTGRES_PASSWORD=secure_password_placeholder
POSTGRES_DB=dagster_db

# Network alias for the database container.
# Must match the service name in docker-compose.yml.
DAGSTER_POSTGRES_HOST=postgres

# --- Docker Orchestration ---
# Sets the custom name for the container group in Docker Desktop.
# Prevents Docker from defaulting to the folder name.
# Change this to customize how it appears in the dashboard, should be LOWERCASE.
COMPOSE_PROJECT_NAME=your_preferred_lowercase_name
